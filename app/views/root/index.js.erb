mostRecentItem = <%= @mostRecentItem %>;

$('#stream').prepend("<%= escape_javascript(render(:partial => 'show_items', :locals => {:items => @recentItems}))%>")

<% if @shortlist != [] %>
  $('#shortlist').empty();
  $('#shortlist').prepend("<%= escape_javascript(render(:partial => 'show_items', :locals => {:items => @shortlist}))%>");
<% end %>
$('.item').slideDown();
