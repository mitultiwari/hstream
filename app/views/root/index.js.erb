mostRecentItem = <%= @mostRecentItem %>;

$('#stream').prepend("<%= escape_javascript(render2 :partial => 'show_items', :items => @recentItems)%>")

<% if @shortlist != [] %>
  $('#shortlist').empty();
  $('#shortlist').prepend("<%= escape_javascript(render2 :partial => 'show_items', :items => @shortlist)%>");
<% end %>
$('.item').slideDown();
