mostRecentItem = <%= @mostRecentItem %>;

<% if @items[:shortlist] %>
shortlist.push.apply(shortlist,
    <%= raw @items[:shortlist].collect{|x| x.hnid.to_s}.inspect%>);
<% end %>

for (var i = 0; i < shortlist.length; ++i) {
  $('.follow[author='+shortlist[i]+']').html('-');
}
